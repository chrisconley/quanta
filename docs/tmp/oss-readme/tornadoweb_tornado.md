# Analysis: tornadoweb/tornado

- **Repo**: tornadoweb/tornado (~22.3k stars)
- **Type**: Python web framework / async networking library
- **Length**: ~1.6 KB, reStructuredText (not Markdown)

## Sections

1. Title (rst-underline style)
2. Gitter badge
3. One-paragraph description anchoring the framework in its history (FriendFeed) and use cases (long polling, WebSockets)
4. "Hello, world" code example (Python)
5. Documentation pointer

No features list, no install instructions, no license — the README defers heavily to the external website.

## Tone & style

- **Register**: understated, veteran-library confident. Assumes the reader already has context.
- **Voice**: third-person impersonal.
- **Format**: RST with section underlines; sparse; no emoji, no marketing copy.

## Notable conventions

- A single runnable code snippet acts as the value proposition instead of a feature list — "this is what it looks like, judge for yourself."
- Link out to a richer demo (chat room) rather than inline more code.
- README is explicitly minimal; `tornadoweb.org` carries the weight.

## Takeaway pattern

Illustrates the "mature project" aesthetic: very little on GitHub, everything on the project site. Works because the audience is pre-qualified. Would be inadequate for attracting new users — the README is a signpost, not a pitch.
